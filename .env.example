# Variable de entorno que necesita la función serverless (/api/generate-informe.js)
# Se configura en Vercel → Project Settings → Environment Variables.
# Este archivo es solo de referencia, no se sube con datos reales.
ANTHROPIC_API_KEY=sk-ant-...
